---
- name: Allow AutoFS to mount NFS shares in home directories
  ansible.posix.seboolean:
    name: use_nfs_home_dirs
    state: true
    persistent: true

- name: Allow remote MySQL Connections
  ansible.posix.seboolean:
    name: httpd_can_network_connect_db
    state: true
    persistent: true
