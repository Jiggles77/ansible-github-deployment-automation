---
- name: Open SMTP port in firewall
  firewalld:
    service: smtp
    permanent: true
    state: enabled
  notify: Reload Firewalld

- name: Open IMAP port in firewall
  firewalld:
    service: imap
    permanent: true
    state: enabled
    immediate: true
  notify: Reload Firewalld
