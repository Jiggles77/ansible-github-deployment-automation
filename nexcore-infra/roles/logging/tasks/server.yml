---
- name: Configure Rsyslog on central logging server to listen for client logs 
  template:
    src: imtcp.conf.j2
    dest: /etc/rsyslog.d/imtcp.conf
    owner: root
    group: root
    mode: '0755'
  notify: Restart Rsyslog
